Voce esta trabalhando na task SEC-006 deste repositorio.

Contexto:
- A task aplica rate limit aos fluxos criticos.
- O reviewer bloqueou anteriormente por:
  1. ausencia de evento de seguranca agregado para bloqueios repetidos;
  2. consumo do rate limit sendo revertido junto com transacao de negocio;
  3. Server Actions bloqueadas retornando resultado que formularios nativos ignoravam, causando sucesso silencioso.
- Esses problemas anteriores NAO devem ser reintroduzidos.

Estado atual:
- Existe um teste novo:
  src/tests/rate-limited-document-form.test.tsx
- O teste tenta provar feedback visivel ao usuario quando o formulario real de upload de documentos recebe RATE_LIMIT_EXCEEDED.
- Ja houve varias tentativas manuais de mocks.
- O teste atualmente nao consegue acionar de forma confiavel o fluxo React 19 de <form action={function}> / useActionState em JSDOM.
- Nao queremos continuar remendando mocks aleatoriamente.

Sua tarefa:
1. Leia e entenda antes de alterar:
   - docs/06-security-and-rls.md
   - src/lib/rate-limit/**
   - src/app/(private)/app/documentos/page.tsx
   - src/features/documents/actions.ts
   - src/tests/rate-limited-document-form.test.tsx
   - qualquer componente novo de formulario/action feedback criado nesta task
   - package.json e config do Vitest
2. Determine qual e a arquitetura real implementada para feedback de RATE_LIMIT_EXCEEDED em Server Actions.
3. Corrija a implementacao e/ou o teste de forma correta, estavel e coerente com React 19.
4. O teste deve provar comportamento de usuario real na medida suportada pelo ambiente:
   - um formulario protegido recebe resultado RATE_LIMIT_EXCEEDED;
   - feedback visivel e acessivel aparece para o usuario;
   - o write nao ocorre.
5. Evite testar novamente toda a logica interna de documentos se isso nao for necessario.
6. Nao enfraqueca o teste apenas para faze-lo passar.
7. Nao remova a cobertura de rate limit dos entrypoints criticos ja existente.
8. Se JSDOM nao suportar adequadamente o mecanismo de form actions do React 19, teste o componente reutilizavel de feedback diretamente com uma action controlada, e mantenha uma verificacao separada de que o formulario real usa esse componente/wrapper. Explique isso em comentario curto no teste.
9. Corrija o warning:
   "Cannot specify a encType or method for a form that specifies a function as the action"
   sem quebrar upload de arquivo via FormData.
10. Se @testing-library/user-event foi adicionado apenas durante a tentativa manual e nao for necessario na solucao final, remova a dependencia e ajuste package-lock.json corretamente.
11. Nao use DATABASE_DIRECT_URL nem bypass de RLS para resolver qualquer coisa aqui.
12. Nao faca alteracoes de Git:
   - nao commit
   - nao merge
   - nao checkout
   - nao reset
   - nao push
13. Ao final, rode:
   - npx vitest run src/tests/rate-limited-document-form.test.tsx
   - npm run typecheck
   - npm run lint
   - npm run test
   - git diff --check
14. Se algum comando falhar por sandbox/EPERM, registre isso, mas nao invente sucesso.

Objetivo final:
deixar a worktree pronta para o orchestrator revisar novamente, com uma solucao arquitetural e um teste confiavel, e nao apenas outro mock pontual.